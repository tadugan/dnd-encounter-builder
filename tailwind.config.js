module.exports = {
    purge: ['./src/**/*.tsx', './src/**/*.js'], // TODO: Might need to add more routes
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
